{
  "name": "unstable-truck",
  "version": "0.1.0",
  "private": true,
  "description": "Daily speed-run truck delivery game with procedurally generated road networks.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python -m http.server 8123"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
