{
  "name": "unstable-truck",
  "version": "0.1.0",
  "private": true,
  "description": "Daily speed-run truck delivery game with procedurally generated road networks.",
  "type": "module",
  "scripts": {
    "build": "npm run build:client && npm run build:server",
    "build:client": "tsc",
    "build:server": "tsc -p server/tsconfig.json",
    "watch": "tsc --watch",
    "start": "node server/dist/index.js",
    "serve": "python -m http.server 8123",
    "test": "npm run build:client && node scripts/collision-check.mjs && node scripts/storage-check.mjs && node scripts/medal-check.mjs && node scripts/medal-par-check.mjs && node scripts/split-check.mjs && node scripts/tutorial-check.mjs && node scripts/replay-check.mjs && node scripts/seed-target-check.mjs"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.2",
    "@types/pg": "^8.20.3",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.22.0"
  }
}
